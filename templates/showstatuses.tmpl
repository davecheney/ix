{{ template "header" "Statues" }}
  <div class="page-header">
    <h1>Statuses <small></small></h1>
  </div>
  <div class="list-group">
  {{ range .Statuses }}
    <a class="list-group-item" href="/status/{{ . }}">{{ . }}</a>
  {{ end }} 
  </div>
{{ template "footer" }}
